use std::collections::HashMap;

use kasane_logic::{
    geometry::point::coordinate::Coordinate,
    id::space_id::{
        SpaceID,
        constants::{F_MAX, F_MIN, XY_MAX},
        range::RangeID,
        single::SingleID,
    },
};

fn main() {
    println!("{:?}", F_MIN);
}
