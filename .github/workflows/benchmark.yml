name: Memori Benchmark

on:
  push:
    branches:
      - main

jobs:
  memori:
    uses: tomoro0726/memori/.github/workflows/memori-publish.yml@main
    with:
      pages_branch: "memori"
