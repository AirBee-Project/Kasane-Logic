name: Memori Benchmark

on:
  push:
    branches:
      - main

jobs:
  memori:
    uses: Tomoro0726/Memori/blob/master/.github/workflows/memori-publish.yml
    with:
      pages_branch: "memori"
