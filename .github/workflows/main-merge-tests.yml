name: main merge tests

on:
  pull_request:
    branches:
      - main

jobs:
  lints:
    name: Lints
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Setup mold
        uses: rui314/setup-mold@v1

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Rustdoc
        shell: bash
        env:
          RUSTDOCFLAGS: -D warnings
        run: |
          set -euo pipefail
          features=("" "random" "temporal_id" "random,temporal_id")
          for feature in "${features[@]}"; do
            if [ -n "$feature" ]; then
              cargo doc --locked --no-deps --document-private-items --no-default-features --features "$feature"
            else
              cargo doc --locked --no-deps --document-private-items --no-default-features
            fi
          done

      - name: Clippy
        shell: bash
        run: |
          set -euo pipefail
          features=("" "random" "temporal_id" "random,temporal_id")
          for feature in "${features[@]}"; do
            if [ -n "$feature" ]; then
              cargo clippy --locked --lib --bins --tests --no-default-features --features "$feature" -- -D warnings
            else
              cargo clippy --locked --lib --bins --tests --no-default-features -- -D warnings
            fi
          done

  test:
    name: Test ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: lints
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup mold
        if: matrix.os == 'ubuntu-latest'
        uses: rui314/setup-mold@v1

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "test-${{ matrix.os }}"

      - name: Test
        shell: bash
        run: |
          set -euo pipefail
          features=("" "random" "temporal_id" "random,temporal_id")
          for feature in "${features[@]}"; do
            if [ -n "$feature" ]; then
              cargo test --locked --lib --bins --tests --no-default-features --features "$feature"
            else
              cargo test --locked --lib --bins --tests --no-default-features
            fi
          done

      - name: Test ignored
        shell: bash
        run: |
          set -euo pipefail
          features=("" "random" "temporal_id" "random,temporal_id")
          for feature in "${features[@]}"; do
            if [ -n "$feature" ]; then
              cargo test --locked --lib --bins --tests --no-default-features --features "$feature" -- --ignored
            else
              cargo test --locked --lib --bins --tests --no-default-features -- --ignored
            fi
          done

      - name: Doctest
        shell: bash
        run: |
          set -euo pipefail
          features=("" "random" "temporal_id" "random,temporal_id")
          for feature in "${features[@]}"; do
            if [ -n "$feature" ]; then
              cargo test --locked --doc --no-default-features --features "$feature"
            else
              cargo test --locked --doc --no-default-features
            fi
          done